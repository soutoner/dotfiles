# Example environment variables for chezmoi
# Copy this to .env or source before running chezmoi apply

# Your full name (used in git config)
CHEZMOI_NAME="John Doe"

# Your email address (used in git config)
# IMPORTANT: Keep this in .env file, NOT in version control
CHEZMOI_EMAIL="john.doe@example.com"
